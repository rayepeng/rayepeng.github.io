---
import Default from '~/layouts/Default.astro'

/**
 * Projects index. Once `src/content/projects/main.yaml` is populated, this
 * page will iterate the groups dict and render via <ListProjects />.
 */
---

<Default title="Projects - Anthony Fu" description="List of projects that I am proud of">
  <div class="prose m-auto slide-enter-content text-center">
    <h1>Projects</h1>
    <p class="op-50 text-sm"><em>List of projects that I am proud of</em></p>
    <p class="op50 text-sm mt-8 text-left">
      <em>{'⏳ Projects page scaffold — content migration pending. Schema is ready at src/content/config.ts → projectsCollection.'}</em>
    </p>
  </div>
</Default>